# Kernels

The initial normalized radial kernels are Gaussian, Epanechnikov, quartic,
triangular, uniform, and exponential. All integrate to one in every supported
Euclidean dimension.

Kernel shape and bandwidth are separate components. Compact kernels have zero
support beyond one standardized bandwidth; Gaussian and exponential kernels
have infinite support.
