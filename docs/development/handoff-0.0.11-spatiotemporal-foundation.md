# 0.0.11 spatiotemporal-foundation handoff

The complete recoverable record is maintained at:

`HANDOFF_0.0.11_SPATIOTEMPORAL_FOUNDATION.md`

Version 0.0.11 establishes explicit linear/cyclic time, immutable ordinary
space-time events and measured support, reusable distance assets, separable
product-kernel `SpatiotemporalKDE`, optional xarray export, and deterministic
joint/separate weighted LOO bandwidth experiments.

The central numerical rule is that space and time remain separate domains with
separate units, kernels, metrics, and bandwidths. Cyclic kernels use periodic
image sums rather than minimum circular distance alone.

See the root handoff for:

- complete mathematical definitions;
- source and test file maps;
- ownership, metadata, and compatibility rules;
- rejected alternatives and deliberate limitations;
- final local validation and GitHub state;
- the exact 0.0.12 network-time implementation order.
