# Spatial KDE

`SpatialKDE` estimates a normalized density or event intensity in arbitrary
Euclidean dimension. Bandwidth is expressed in the same units as the input
coordinates.

For projected geographic analysis, coordinates should use a suitable projected
CRS rather than longitude and latitude degrees.
