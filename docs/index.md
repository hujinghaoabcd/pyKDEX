# pyKDEX

pyKDEX is a composition-oriented framework for kernel density and event
intensity estimation in Euclidean space, time, linear networks, and their
combinations.

The first baseline provides a rigorously tested `SpatialKDE`. Network and
temporal estimators will be added only after their numerical contracts and
independent validation fixtures are complete.
